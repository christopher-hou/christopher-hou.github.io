{
  "name": "roth-calculator",
  "version": "1.0.0",
  "private": true,
  "description": "Roth vs Traditional 401(k) calculator — static, no build step",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
